<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.map.mapper.mapMapper">
	<!--첨푸파일 개수 카운트 -->
	<select id="selectGeoJsonLcbd100l" resultType="String">
		SELECT 
			ST_AsGeoJSON(geom)
		FROM 
			nsid_alc.lcbd100l
	</select>
</mapper>